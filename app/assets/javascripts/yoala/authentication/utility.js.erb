var Yoala = Yoala || {};
Yoala.authentication = Yoala.authentication || {};

/*
Authentication Module
********************************/

Yoala.authentication.utility = {
  create: function(args={}) {
    $.ajax({
      url: '/authentications',
      type: 'POST',
      data: {
        authentication: {
          provider: args.provider,
          provider_user_id: args.response.userID,
          token: args.response.accessToken,
          expires_at: args.response.expiresIn
        }
      },
      dataType: 'json',
      success: function(data){
        console.log(data);
      }
    });
  }
}
