(function() {
  var $el = $('#comment-flag-<%= @comment.id %>');
  var update_link = '<%= organization_campaign_post_comment_path(@organization, @campaign, @post, @comment, comment: { flagged: !@comment.flagged }) %>';
  App.dataTables.flagToggle($el, <%= @comment.flagged? %>, update_link);
})();
