<%= render "shared/datatables_search", table_id: 'groala-organizations-table' %>
<table id="groala-organizations-table" class="table table-striped table-bordered groala-datatable" cellspacing="0" width="100%">
  <thead>
    <tr>
      <th class="text-xs-center">
        <i class="fa fa-bank grey-text" aria-hidden="true"></i>
        <span class="hidden-xs-down">Organization</span>
      </th>
      <th class="text-xs-center">
        <i class="fa fa-hashtag grey-text" aria-hidden="true"></i>
        <span class="hidden-xs-down">Campaigns</span>
      </th>
      <th class="text-xs-center">
        <i class="fa fa-users grey-text" aria-hidden="true"></i>
        <span class="hidden-xs-down">Users</span>
      </th>
      <th data-orderable="false"></th>
    </tr>
  </thead>
  <tbody>
    <% organizations.each do |organization| %>
      <tr id="organization-<%= organization.id %>">
        <td><%= link_to organization.name, organization_path(organization), class: ' groala-standard-link' %></td>
        <td class="text-xs-center"><%= number_with_delimiter(organization.campaigns.count) %></td>
        <td class="text-xs-center groala-no-wrap"><%= number_with_delimiter(organization.users.count) %></td>
        <td class="text-xs-center groala-no-wrap">
          <%= link_to organization_path(organization) do %>
            <%= content_tag(:i, nil, :class => 'fa fa-eye fa-lg', aria: { hidden: true }, data: { toggle: 'tooltip', placement: 'top' }, title: 'View') %>
          <% end %>
          <%= link_to edit_organization_path(organization) do %>
            <%= content_tag(:i, nil, :class => 'fa fa-pencil fa-lg purple-text', aria: { hidden: true }, data: { toggle: 'tooltip', placement: 'top' }, title: 'Edit') %>
          <% end %>
          <%= link_to organization_path(organization), method: :delete, data: { confirm: 'Are you sure you want to delete this organization? All of its campaigns, posts, and data will be deleted as well.' } do %>
            <%= content_tag(:i, nil, :class => 'fa fa-times fa-lg red-text', aria: { hidden: true }, data: { toggle: 'tooltip', placement: 'top' }, title: 'Delete') %>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
