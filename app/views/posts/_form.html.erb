<%= form_for([organization, campaign, post]) do |f| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :network_id %>
    <%= f.text_field :network_id %>
  </div>

  <div class="field">
    <%= f.label :network_post_id %>
    <%= f.text_field :network_post_id %>
  </div>

  <div class="field">
    <%= f.label :network_parent_id %>
    <%= f.text_field :network_parent_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
