<%= render "shared/datatables_search", table_id: 'facebook-reactions-table' %>
<table id="facebook-reactions-table" class="table table-bordered groala-datatable" cellspacing="0" width="100%" data-source="<%= organization_campaign_post_reactions_url(organization, campaign, post, format: :json) %>">
  <thead class="thead-default">
    <tr>
      <th>Reaction</th>
      <th style="min-width: 110px;">User</th>
      <th class="text-xs-center">
        <i class="fa fa-flag fa-lg" aria-hidden="true"></i>
      </th>
    </tr>
  </thead>
  <tbody>
  </tbody>
</table>
<div class="mt-2">
  <hr>
  <%= link_to organization_campaign_post_reactions_path(organization, campaign, post, format: :csv), class: 'btn btn-md secondary-color', data: { toggle: 'tooltip', placement: 'top' }, title: 'Download Reactions' do %>
    <i class="fa fa-download left"></i> <b>CSV</b>
  <% end %>
</div>
