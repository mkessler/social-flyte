<%= render "shared/datatables_search", table_id: 'facebook-reactions-table' %>
<table id="facebook-reactions-table" class="table table-striped table-bordered groala-datatable" cellspacing="0" width="100%" data-source="<%= organization_campaign_post_reactions_url(organization, campaign, post, format: :json) %>">
  <thead>
    <tr>
      <th class="text-xs-center">
        <i class="fa fa-heart grey-text" aria-hidden="true"></i>
        <span class="hidden-xs-down">Reaction</span>
      </th>
      <th class="text-xs-center">
        <i class="fa fa-user grey-text" aria-hidden="true"></i>
        <span class="hidden-xs-down">User</span>
      </th>
      <th></th>
    </tr>
  </thead>
  <tbody>
  </tbody>
</table>
<%= render 'shared/datatables_buttons', csv_download_link: organization_campaign_post_reactions_path(organization, campaign, post, format: :csv), csv_object: 'Reactions' %>
