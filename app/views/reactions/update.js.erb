(function() {
  var $el = $('a[href="<%= organization_campaign_post_reaction_path(@organization, @campaign, @post, @reaction) %>"]');
  App.dataTables.flagToggle($el, <%= @reaction.flagged? %>);
})();
