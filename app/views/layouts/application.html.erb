<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <title>Yoala</title>
    <%= csrf_meta_tags %>
    <link href="https://fonts.googleapis.com/css?family=Ubuntu" rel="stylesheet">
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body class="fixed-sn blue-grey lighten-5">
    <header>
      <%= render 'shared/navigation/side' %>
      <%= render 'shared/navigation/top' %>
    </header>

    <main>
      <div class="container-fluid">
        <!-- <ul>
          <%= render 'devise/menu/registration_items' %>
          <%= render 'devise/menu/login_items' %>
          <li>
            <a class="nav_link" href="#" onclick="App.authentications.facebook.login(); return false;">Facebook Auth</a>
          </li>
        </ul> -->
        <%= render_breadcrumbs builder: ::BootstrapBreadcrumbsBuilder %>
        <%= yield %>
      </div>
    </main>

    <%= javascript_include_tag 'mdb/mdb.min' %>
    <%= render 'shared/toastr_notifications' %>
  </body>
</html>
