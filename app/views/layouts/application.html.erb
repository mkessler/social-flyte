<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <%= display_meta_tags %>
    <%= icons_meta_tags %>
    <%= csrf_meta_tags %>
    <link href="https://fonts.googleapis.com/css?family=Ubuntu:300,400,500,700" rel="stylesheet">
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body class="fixed-sn grey lighten-2">
    <header>
      <%= render 'shared/navigation/side' %>
      <%= render 'shared/navigation/top' %>
    </header>

    <div class="container-fluid breadcrumb-container rgba-white-strong">
      <%= render_breadcrumbs builder: ::BootstrapBreadcrumbsBuilder %>
    </div>
    <main>
      <div class="container-fluid">
        <div class="dashboard-title-wrapper">
          <%= yield :page_title %>
        </div>
        <%= yield %>
      </div>
    </main>

    <footer class="page-footer center-on-small-only">
      <div class="footer-copyright">
        <div class="container-fluid">
          <%= link_to '<i class="fa fa-paper-plane" aria-hidden="true"></i> SocialFlyte'.html_safe, root_url %> <small><i class="fa fa-copyright" aria-hidden="true"></i> <%= Time.now.year %></small>
        </div>
      </div>
    </footer>
    <%= javascript_include_tag 'mdb/mdb.min' %>
    <%= render 'shared/toastr_notifications' %>
  </body>
</html>
